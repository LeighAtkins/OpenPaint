HF_TOKEN=hf_xxx_your_token_here
HF_SPACE_URL=https://kenjiedec-rembg.hf.space
PORT=8787
HF_FN_INDEX=

# Notes:
# - HF_TOKEN is optional for public Spaces, but recommended to avoid rate limits.
# - HF_SPACE_URL should point to the RemBG Space you want to use.
# - PORT is the local port for the proxy server.
# - HF_FN_INDEX (optional) forces a specific Gradio fn_index if auto-discovery fails.
