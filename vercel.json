{
  "installCommand": "npm install --ignore-scripts",
  "buildCommand": "npm run vercel-build",
  "outputDirectory": "dist",
  "functions": {
    "api/[...all].js": {
      "includeFiles": "server/**"
    }
  }
}
